// @generated by hnk-core. Manual edits in generated files will be overwritten.

use super::hooks::{metadata, StubbornSenderHandler};
use super::state::StubbornSenderState;
use super::super::runtime_contracts::{LocalDispatch, MustReplyHooks, NonLocalDispatch, RuntimeHooks, TimeoutHooks};

pub struct StubbornSenderWiring<'a, H, R, M, T, L, N> {
    pub handler: &'a mut H,
    pub runtime_hooks: &'a mut R,
    pub must_reply_hooks: &'a mut M,
    pub timeout_hooks: &'a mut T,
    pub local_dispatch: &'a mut L,
    pub non_local_dispatch: &'a mut N,
}

impl<'a, H, R, M, T, L, N> StubbornSenderWiring<'a, H, R, M, T, L, N>
where
    H: StubbornSenderHandler,
    R: RuntimeHooks,
    M: MustReplyHooks,
    T: TimeoutHooks,
    L: LocalDispatch,
    N: NonLocalDispatch,
{
    pub fn run_transition(&mut self, transition: &str, state: &mut StubbornSenderState) {
        let meta = metadata("StubbornSender", None, None, false);
        self.runtime_hooks.on_transition_enter(&meta);
        match transition {
            "clear_after_ack" => self.handler.handle_clear_after_ack(state),
            "retransmit_after_timeout" => self.handler.handle_retransmit_after_timeout(state),
            "send_first_time" => self.handler.handle_send_first_time(state),
            _ => {}
        }
        self.runtime_hooks.on_transition_exit(&meta);
    }

    pub fn dispatch_network_out(&mut self) {
        let meta_link_message = metadata("StubbornSender", Some("network_out"), Some("LinkMessage"), false);
        self.runtime_hooks.on_send(&meta_link_message);
        self.must_reply_hooks.on_request_started(&meta_link_message);
        // If runtime determines the reply was never fulfilled, call:
        // self.must_reply_hooks.on_reply_missing(&meta_link_message);
        self.timeout_hooks.on_timeout_window_started(&meta_link_message, "3s");
        // If runtime observes timeout violation, call:
        // self.timeout_hooks.on_timeout_contract_violation(&meta_link_message, "3s");
        let boundary_meta = metadata("StubbornSender", Some("network_out"), None, true);
        self.runtime_hooks.on_non_local_dispatch(&boundary_meta);
        self.non_local_dispatch.dispatch_non_local(&boundary_meta);
        // target actor: receiver-runtime
    }

    pub fn observe_network_in_link_ack(&mut self) {
        let meta = metadata("StubbornSender", Some("network_in"), Some("LinkAck"), false);
        self.runtime_hooks.on_receive(&meta);
    }

    pub fn observe_retry_in_retry_timeout(&mut self) {
        let meta = metadata("StubbornSender", Some("retry_in"), Some("RetryTimeout"), false);
        self.runtime_hooks.on_receive(&meta);
        self.timeout_hooks.on_timeout_event_fired(&meta);
    }

    pub fn observe_start_in_start_send(&mut self) {
        let meta = metadata("StubbornSender", Some("start_in"), Some("StartSend"), false);
        self.runtime_hooks.on_receive(&meta);
    }

}
