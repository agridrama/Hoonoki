// @generated by hnk-core. Manual edits in generated files will be overwritten.

use super::hooks::{metadata, FairLossLinkHandler};
use super::state::FairLossLinkState;
use super::super::runtime_contracts::{LocalDispatch, MustReplyHooks, NonLocalDispatch, RuntimeHooks, TimeoutHooks};

pub struct FairLossLinkWiring<'a, H, R, M, T, L, N> {
    pub handler: &'a mut H,
    pub runtime_hooks: &'a mut R,
    pub must_reply_hooks: &'a mut M,
    pub timeout_hooks: &'a mut T,
    pub local_dispatch: &'a mut L,
    pub non_local_dispatch: &'a mut N,
}

impl<'a, H, R, M, T, L, N> FairLossLinkWiring<'a, H, R, M, T, L, N>
where
    H: FairLossLinkHandler,
    R: RuntimeHooks,
    M: MustReplyHooks,
    T: TimeoutHooks,
    L: LocalDispatch,
    N: NonLocalDispatch,
{
    pub fn run_transition(&mut self, transition: &str, state: &mut FairLossLinkState) {
        let meta = metadata("std.link.FairLossLink", None, None, "local");
        self.runtime_hooks.on_transition_enter(&meta);
        match transition {
            "forward_once" => self.handler.handle_forward_once(state),
            _ => {}
        }
        self.runtime_hooks.on_transition_exit(&meta);
    }

    pub fn dispatch_deliver_out(&mut self) {
        let meta_std_link_fair_loss_deliver = metadata("std.link.FairLossLink", Some("deliver_out"), Some("std.link.FairLossDeliver"), "local");
        self.runtime_hooks.on_send(&meta_std_link_fair_loss_deliver);
        // No declared propagation paths for this port.
    }

    pub fn observe_send_in_std_link_fair_loss_send(&mut self) {
        let meta = metadata("std.link.FairLossLink", Some("send_in"), Some("std.link.FairLossSend"), "local");
        self.runtime_hooks.on_receive(&meta);
    }

}
